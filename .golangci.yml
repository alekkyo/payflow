version: "2"

run:
  timeout: 5m

linters:
  settings:
    errcheck:
      check-type-assertions: true

issues:
  exclusions:
    rules:
      - path: "_test\\.go"
        linters:
          - errcheck
